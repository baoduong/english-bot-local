[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    fast: Fast unit tests.
    slow: Long-running tests.
    integration: Multi-component integration tests.
    fault_injection: Error-path and resilience tests.
